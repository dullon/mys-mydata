19 接口只用于定义类型
这条针对 常量接口 ： 即 接口中有常量定义
 如此定义后  实现类就会把在内部使用常量的实现细节 泄露到导出该类的API中。
 所以这样的接口不可取。
 常用的办法 是 把 这些常量单独定义到不可实例化的工具类里，调用时 用类名修饰常量，或者静态导入。


20 类层次优于标签类
子类化优于类内加杂标签-是java面向对象的优势的一种，子类化易于扩展，如果是类内标签，每次扩展都需要修改程序，这样违反了开闭原则。
所以能对象化 就不要将对象 标签化。（枚举类型）